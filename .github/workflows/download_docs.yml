name: Document Archiver

on:
  schedule:
    - cron: "* */2 * * *" # every 2 hours minutes
  workflow_dispatch:

jobs:
  run-archiver:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout storage repo
        uses: actions/checkout@v4
        with:
          repository: kasunwathsala/test-cron-job-archiver-storage
          token: ${{ secrets.TARGET_REPO_PAT }}
          path: storage-repo

      - name: Checkout script repo
        uses: actions/checkout@v4
        with:
          path: script-repo

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: pip install requests

      - name: Run archiver inside storage repo
        run: |
          cp script-repo/main.py storage-repo/
          cd storage-repo
          python main.py
          rm main.py

      - name: Commit and push updates to storage repo
        run: |
          cd storage-repo
          git config user.name "kasunwathsala"
          git config user.email "kasun.20222191.iit.ac.lk"
          git add .
          git commit -m "🔄 Auto-update archive - $(date '+%Y-%m-%d %H:%M:%S')" || echo "No changes to commit"
          git push origin main --force
